const API_URL: { [key: string]: string } = {
  POSTS: 'posts',
  COMMENTS: 'comments',
  ALBUMS: 'albums',
  PHOTOS: 'photos',
  TODOS: 'todos',
  USERS: 'users',
  ERROR_TEST: 'error',
}

export default API_URL
